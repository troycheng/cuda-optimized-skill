interface:
  display_name: "CUDA Kernel Optimizer"
  short_description: "Benchmark, profile, and optimize GPU kernels"
  default_prompt: "Use $cuda-kernel-optimizer to benchmark or optimize this CUDA, CUTLASS, or Triton kernel, or analyze its Nsight Compute report."
