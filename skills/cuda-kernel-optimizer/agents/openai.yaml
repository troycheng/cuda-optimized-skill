interface:
  display_name: "CUDA Kernel Optimizer"
  short_description: "Evidence-driven GPU kernel and workload optimization"
  default_prompt: "Use $cuda-kernel-optimizer to optimize or diagnose this CUDA, CUTLASS, Triton, framework, or serving workload. Establish the readiness and claim ceiling first, load only the relevant workflow and a bounded set of architecture-compatible knowledge cards, require user-provided workload evidence for end-to-end claims, and keep host changes recommend-only. Use current primary-source search or independent model challenge only when policy and network allow it; local correctness and measurement remain authoritative."
